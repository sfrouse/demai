import { WebComponentInfo } from "../../installWebComponents";
import demaiCardCDef from "./demai-card.cdef";
import demaiCardCode from "./demai-card.code";

const demaiCard: WebComponentInfo = {
  id: "demai-card",
  code: demaiCardCode,
  cdef: demaiCardCDef,
};

export default demaiCard;
