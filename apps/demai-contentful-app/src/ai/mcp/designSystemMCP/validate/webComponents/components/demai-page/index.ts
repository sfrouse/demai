import { WebComponentInfo } from "../../installWebComponents";
import demaiPageCDef from "./demai-page.cdef";
import demaiPageCode from "./demai-page.code";

const demaiPage: WebComponentInfo = {
  id: "demai-page",
  code: demaiPageCode,
  cdef: demaiPageCDef,
};

export default demaiPage;
