import { WebComponentInfo } from "../../installWebComponents";
import demaiSectionCDef from "./demai-section.cdef";
import demaiSectionCode from "./demai-section.code";

const demaiSection: WebComponentInfo = {
  id: "demai-section",
  code: demaiSectionCode,
  cdef: demaiSectionCDef,
};

export default demaiSection;
