import { WebComponentInfo } from "../../installWebComponents";
import demaiHeroCDef from "./demai-hero.cdef";
import demaiHeroCode from "./demai-hero.code";

const demaiHero: WebComponentInfo = {
  id: "demai-hero",
  code: demaiHeroCode,
  cdef: demaiHeroCDef,
};

export default demaiHero;
