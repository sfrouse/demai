import createEntryFunction from "./createEntry.function";
import createEntryTool from "./createEntry.tool";

const createEntry = {
  tool: createEntryTool,
  function: createEntryFunction,
};

export default createEntry;
