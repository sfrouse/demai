import createContentTypeFunction from "./createContentType.function";
import createContentTypeTool from "./createContentType.tool";

const createContentType = {
  tool: createContentTypeTool,
  function: createContentTypeFunction,
};

export default createContentType;
