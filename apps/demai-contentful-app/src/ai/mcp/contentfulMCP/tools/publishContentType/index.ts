import { default as publishContentTypeTool } from "./publishContentType.tool";
import { default as publishContentTypeFunction } from "./publishContentType.function";

const publishContentType = {
  tool: publishContentTypeTool,
  function: publishContentTypeFunction,
};

export default publishContentType;
